# Each of these starts a Postgres container; unbounded parallelism starves the Docker daemon.
[test-groups.postgres]
max-threads = 4

[[profile.default.overrides]]
filter = 'kind(test)'
test-group = 'postgres'

[profile.default]
slow-timeout = { period = "60s", terminate-after = 8 }
